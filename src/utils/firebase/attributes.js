export const getSlug = (item) => {
  return item ? item.slug : null
}

export const getTitle = (item) => {
  return item ? item.slug : null
}

export const getOverview = (item) => {
  return item ? item.slug : null
}

export const getProfileBio = (item) => {
  return item ? item.slug : null
}

export const getAuthor = (item) => {
  return item ? item.author : null
}

export const getCoverImage = (item) => {
  return item ? item.image : null
}

export const getProfileImage = (item) => {
  return item ? item.image : null
}
